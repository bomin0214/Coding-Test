풀이
특정 시간 mid가 주어졌을 때 Σ (mid / times[i]) 로 처리 가능한 인원을 구하고,
n명 이상이면 시간을 줄이고, 미만이면 시간을 늘리는 이분 탐색으로 최솟값을 찾는다.
탐색 범위는 0 ~ max(times) * n (가장 느린 심사관이 혼자 n명 처리하는 최악의 시간).

시행 착오
처음엔 우선순위 큐로 시뮬레이션 시도 — n이 최대 10억이라 반복문 자체가 시간 초과
오버플로우 — times[i]와 n 모두 최대 10억이므로 곱할 때 (long) 캐스팅 필요, complete도 long으로 선언해야 함
