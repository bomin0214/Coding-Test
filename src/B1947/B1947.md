# 풀이에 대한 설명

완전순열 문제라서 점화식 dp[n] = (n-1)*(dp[n-1]+dp[n-2])을 이용해서 풀었다.
작은 값부터 DP로 구하고, 값이 커지니까 매번 MOD를 적용했다.

# 시행 착오

처음에는 직접 경우를 세려고 했는데 너무 오래 걸렸다.
또 dp[n-1]만 써서 틀렸고, MOD를 마지막에만 적용해서 오답이 나왔다.

→ 점화식 제대로 쓰고, 계산할 때마다 MOD 적용하도록 수정했다.
