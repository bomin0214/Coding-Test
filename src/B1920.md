### 문제 설명
- N개의 정수가 주어지고, M개의 정수에 대해 해당 값이 존재하는지 확인하는 문제
- 각 질의마다 존재하면 1, 없으면 0을 출력해야 함
- 입력 크기가 크기 때문에 효율적인 탐색 방법이 필요함

### 원인 분석
- 단순 반복문(O(N))으로 탐색할 경우  
  → 전체 시간 복잡도 O(N × M) 발생  
  → 입력 크기가 클 경우 시간 초과 발생 가능

## 해결 방법
- HashSet 사용하여 탐색 시간 단축하기.
- 해시셋은 O(1)의 탐색 속도를 가진다.
